apps:
  - script: ./index.js
    args: '6643'
    watch:
      - 'node'
      - '.env'
    error_file: logs/app.error.log
    out_file: logs/app.output.log
    combine_logs: true
    log_date_format: 'YYYY-MM-DD HH:mm Z'
    env:
      NODE_PATH: './'
      NODE_DEBUG: ''
